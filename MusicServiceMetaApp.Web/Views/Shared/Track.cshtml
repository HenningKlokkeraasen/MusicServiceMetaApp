@model Msma.Domain.Dtos.TrackDto

@{
    ViewBag.Title = Model.Track.Name + " (" + Model.Source + ")";
    ViewBag.Source = Model.Source;
}

@{ Html.RenderPartial("DisplayTemplates/Track", Model); }

@{Html.RenderPartial("DisplayTemplates/Source", Model.Source); }