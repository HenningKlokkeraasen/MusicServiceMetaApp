@model Msma.Domain.Dtos.ArtistDto

@{
    ViewBag.Title = Model.Artist.Name + " (" + Model.Source + ")";
    ViewBag.Source = Model.Source;
}

@{ Html.RenderPartial("DisplayTemplates/Artist", Model); }

@{Html.RenderPartial("DisplayTemplates/Source", Model.Source); }